# Code owners for CC Switch X
#
# Once branch protection is enabled on public `main`, configure it to require
# review from Code Owners. With that rule active, a PR cannot merge without an
# approval from the owner below.

# Global fallback: every PR needs owner approval before merging to main.
*                 @haochen-leo

# Most sensitive paths — CI/signing/release and the Rust backend. Listed
# explicitly so they stay locked even if the global rule above is relaxed later.
/.github/         @haochen-leo
/src-tauri/       @haochen-leo
